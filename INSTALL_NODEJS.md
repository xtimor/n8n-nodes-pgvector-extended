# 🟢 Как установить Node.js на macOS

## Рекомендуемый способ: Homebrew (самый простой)

### Шаг 1: Установите Homebrew (если ещё не установлен)

Откройте Terminal и выполните:

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

### Шаг 2: Установите Node.js

```bash
brew install node
```

### Шаг 3: Проверьте установку

```bash
node -v
npm -v
```

Должны увидеть версии (например, `v20.x.x` и `10.x.x`)

---

## Альтернативный способ 1: Официальный установщик

1. Перейдите на https://nodejs.org/
2. Скачайте **LTS версию** (рекомендуется)
3. Откройте `.pkg` файл
4. Следуйте инструкциям установщика
5. Перезапустите Terminal
6. Проверьте: `node -v` и `npm -v`

---

## Альтернативный способ 2: NVM (для разработчиков)

NVM позволяет управлять несколькими версиями Node.js.

### Установка NVM:

```bash
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
```

### Перезапустите Terminal, затем:

```bash
# Установить последнюю LTS версию
nvm install --lts

# Использовать эту версию
nvm use --lts

# Проверить
node -v
npm -v
```

---

## После установки Node.js

### Выполните установку проекта:

```bash
cd /Users/xtimor/Documents/PGVectorStore
./install.sh
```

Этот скрипт автоматически:
- ✅ Проверит версию Node.js
- ✅ Установит зависимости
- ✅ Соберет проект
- ✅ Предложит создать link для n8n

---

## Решение проблем

### Если команда `node` не найдена после установки:

1. **Перезапустите Terminal**
2. Проверьте PATH:
   ```bash
   echo $PATH
   ```
3. Для Homebrew добавьте в `~/.zshrc`:
   ```bash
   export PATH="/usr/local/bin:$PATH"
   ```
   Затем:
   ```bash
   source ~/.zshrc
   ```

### Если нужны права администратора:

При установке через Homebrew или официальный установщик может потребоваться пароль администратора.

---

## Быстрая проверка

После установки выполните:

```bash
node --version && npm --version && echo "✅ Node.js успешно установлен!"
```

---

## Следующие шаги после установки Node.js

1. ✅ Установите зависимости проекта: `./install.sh`
2. ✅ Настройте PostgreSQL: `./scripts/setup-postgres.sh`
3. ✅ Свяжите с n8n
4. ✅ Протестируйте ноду в n8n

Подробнее см. в [walkthrough.md](file:///Users/xtimor/.gemini/antigravity/brain/b96275cb-34e3-422a-8b64-ed76c7b1b2f4/walkthrough.md)
