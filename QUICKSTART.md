# 🚀 Быстрый старт — Postgres Vector Store Tool

Полное руководство от установки Node.js до использования инструмента AI агента в n8n.

## ✅ Шаг 1: Установите Node.js

> [!IMPORTANT]
> Требуется Node.js 18 или выше.

Самый простой способ через Homebrew:
```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
brew install node
node -v && npm -v
```

Другие варианты описаны в [INSTALL_NODEJS.md](INSTALL_NODEJS.md).

---

## ✅ Шаг 2: Соберите проект

```bash
cd /workspace/n8n-nodes-pgvector-extended
./install.sh
```

Скрипт установит зависимости и соберёт проект.

---

## ✅ Шаг 3: Настройте PostgreSQL

```bash
./scripts/setup-postgres.sh
```

Скрипт создаст базу, таблицы и расширение pgvector. При желании можно выполнить `psql -f scripts/setup-postgres.sql` вручную.

---

## ✅ Шаг 4: Установите ноду в n8n

### Вариант A: npm link
```bash
npm link
cd ~/.n8n/custom
npm link n8n-nodes-postgres-vector-store-tool
```

### Вариант B: Ручное копирование
```bash
mkdir -p ~/.n8n/custom
cp -r dist/* ~/.n8n/custom/
```

Перезапустите n8n после установки.

---

## ✅ Шаг 5: Настройте credentials в n8n

1. Откройте n8n → **Settings → Credentials**.
2. Добавьте **Postgres** (стандартные креденшалы n8n).
3. Заполните Host, Database, User, Password, Port и SSL по вашей среде.

---

## ✅ Шаг 6: Подключите embedding и настройте режим

1. Создайте workflow с вашим AI агентом.
2. Подключите embedding-ноду к входу **Embedding** инструмента.
3. В Postgres Vector Store Tool выберите **Mode → Retrieving with RLS Role** или **Custom SQL Query**.
   - Для Retrieval задайте **RLS Role**, **Table Name**, **Limit**, **Include Metadata** и при необходимости переименуйте столбцы в **Options → Column Names**.
   - Для Custom SQL укажите только поле **SQL Query**.
4. В поле **Description** опишите для агента, как следует использовать инструмент.

Теперь инструмент готов к использованию как часть AI агента в n8n!
